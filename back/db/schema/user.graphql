extend type Query {
  Users: [User!]!
  User(id: ID!): User
}

extend type Mutation {
  UpdateUser(name: String!): Response!
  DeleteUser: Response!
}

type User {
  id: ID!
  name: String!
}

extend type Response {
  success: Boolean!
  message: String
}